import React from "react";
import {Chart} from "react-charts";

const SampleChart = () => {
        return (
            <div>
                <p>This is where the sample chart will go.</p>
            </div>
        );
}

export default SampleChart;